package Boletin8;
/**
 * Mostra a lonxitude do texto: “Isto é Java!”
 * (Muestra la longitud del texto)
 * @author Karly Albarrán
 */
public class eje1 {

    // Método principal que se ejecuta al iniciar el programa
    public static void main(String[] args) {

        // Se declara una variable tipo String con el texto deseado
        String texto = "Isto é java";

        // Se imprime en consola la longitud del texto usando .length()
        // Esto cuenta todos los caracteres, incluidos los espacios
        System.out.println(texto.length());

    }
}
